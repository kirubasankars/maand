#!/bin/bash
rsync -ravc --stats --human-readable --rsh="ssh -i /workspace/$SSH_KEY -o StrictHostKeyChecking=no -o LogLevel=error -l $SSH_USER" --delete --filter='merge /tmp/rsync_rules.txt' $AGENT_DIR/ "$AGENT_IP":/opt/agent/ > /dev/null